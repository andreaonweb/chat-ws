{
  "name": "chat-websocket",
  "version": "1.0.0",
  "description": "A real-time chat application with WebSockets, Firebase Auth, and AI integration",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend/chat-app && npm run dev\" \"cd backend && pip install -r requirements.txt && python -m uvicorn src.main:app --host 0.0.0.0 --port 8000\"",
    "build": "npm run build",
    "test": "npm test"
  },
  "dependencies": {
    "@angular/core": "17.3.11",
    "@angular/common": "17.3.11",
    "@angular/platform-browser": "17.3.11",
    "@angular/router": "17.3.11",
    "@angular/fire": "17.0.0",
    "rxjs": "~7.8.1",
    "tslib": "^2.6.2"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^17.3.10",
    "typescript": "~5.4.5",
    "@types/node": "^20.0.0",
    "concurrently": "^8.2.0"
  }
}
